"""Shared service-API clients (search, image generation, browser fetch backends).

Used by both ``searchos`` (deep-research harness) and ``quickchat`` (plain
chat) — kept independent of either so it's not nested inside searchos.
"""
