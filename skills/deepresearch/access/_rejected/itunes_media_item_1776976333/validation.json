{
  "candidate": "itunes_media_item",
  "host": "itunes.apple.com",
  "passing": false,
  "gates": [
    {
      "name": "A_agent",
      "passed": false,
      "reason": "params_schema_missing_query",
      "detail": {
        "params_schema": {
          "term": {
            "type": "string",
            "required": true,
            "description": "The URL-encoded text string to search for (e.g., artist, album, or movie title)."
          },
          "entity": {
            "type": "string",
            "required": false,
            "description": "The type of results to return (e.g., album, movie, song, musicArtist)."
          },
          "limit": {
            "type": "integer",
            "default": 5,
            "description": "The maximum number of search results to return."
          }
        }
      }
    }
  ]
}